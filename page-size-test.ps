%!
/in {72 mul} def

%
/W 2.5 in def
/H 3.5 in def

<< /PageSize [W H] >> setpagedevice

/rect {
    /h exch def
    /w exch def
    /y exch def
    /x exch def
    newpath 
    x y moveto
    w 0 rlineto
    0 h rlineto
    w neg 0 rlineto
    closepath
} def

0 0 W H rect fill


showpage